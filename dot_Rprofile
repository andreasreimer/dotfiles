options(scipen = 999)
## Default repo
local({r <- getOption("repos")
       r["CRAN"] <- "https://cloud.r-project.org/" 
       options(repos=r)
})

# .First <- function() {
#     cat(cowsay::say(praise::praise(), by='random'))
#     }

# .Last <- function() {
#     cat('Bye now.')
# }

